'use strict';
// QuizSyncRun model deprecated (choice C: no audit persistence)
// Keeping empty export to avoid require errors if referenced somewhere not cleaned yet.
module.exports = (sequelize, DataTypes) => {
  return {};
};
